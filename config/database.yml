# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
#default: &default
#  adapter: sqlite3
#  pool: 5
#  timeout: 5000

development:
    adapter: mysql
    database: rb_blog_deve
    username: root
    password: admin
    host: localhost

test:
    adapter: mysql
    database: rb_blog_test
    username: root
    password: admin
    host: localhost

production:
    adapter: mysql
    database: rb_blog_prod
    username: root
    password: admin
    host: localhost
